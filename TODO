// SPDX-License-Identifier: MIT
pragma solidity ^0.5.0;
contract TECH4DEV{
   /* struct todo{
        string title;
        bool completed;
    }
//array of struct//
todo[] public tech;
//classwork: create a function that insert title and false into the array of struct//
function insert( string memory _title) public{
tech. push(todo(_title, false));

*/

//classwork://

struct blessing{
        string Name;
        bool completed;
    }
 blessing[] public gloria;
 function insert( string memory _Name, bool _completed) public{
gloria.push(blessing(_Name, _completed));

 }
}

